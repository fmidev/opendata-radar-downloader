services:
  downloader:
    image: ghcr.io/fmidev/opendata-radar-downloader:latest
    build: .
    volumes:
      - ./data:/data
    environment:
      - OUTPUT_DIR=/data
    restart: unless-stopped
